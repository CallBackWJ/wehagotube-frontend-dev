import React from 'react';
import PropTypes from 'prop-types';

const Watch = props => {
    return (
        <div>
            
        </div>
    );
};

Watch.propTypes = {
    
};

export default Watch;